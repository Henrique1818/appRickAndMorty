import { Dimensions } from 'react-native';

export const espacamento = {
  extraGrande: 48,
  grande: 36,
  medio: 24,
  pequeno: 12,
  extraPequeno: 8,
};

export const bordaArredondar = 6;

export const viewport = Dimensions.get('window');

export const vw = viewport.width;

export const vh = viewport.height;
